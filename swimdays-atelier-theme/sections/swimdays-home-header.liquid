{% render 'swimdays-home-fonts' %}
{% render 'swimdays-home-header',
  primary_menu: section.settings.primary_menu,
  announcement_enabled: section.settings.announcement_enabled,
  announcement_text_primary: section.settings.announcement_text_primary,
  announcement_text_secondary: section.settings.announcement_text_secondary,
  announcement_link_label: section.settings.announcement_link_label,
  announcement_link_url: section.settings.announcement_link_url,
  launch_announcement_enabled: section.settings.launch_announcement_enabled,
  launch_announcement_first: section.settings.launch_announcement_first,
  launch_announcement_label: section.settings.launch_announcement_label,
  launch_announcement_text: section.settings.launch_announcement_text,
  launch_announcement_link_label: section.settings.launch_announcement_link_label,
  launch_announcement_link_url: section.settings.launch_announcement_link_url,
  launch_announcement_background: section.settings.launch_announcement_background
%}

<div class="swimdays-home-header-spacer" aria-hidden="true"></div>

{% stylesheet %}
  body {
    --swimdays-fixed-header-height: calc(2rem + clamp(60px, 7vw, 72px));
  }

  body.announcement-hidden {
    --swimdays-fixed-header-height: clamp(60px, 7vw, 72px);
  }

  .swimdays-home-header-spacer {
    height: 0;
    width: 100%;
    pointer-events: none;
  }

  body.announcement-hidden .swimdays-home-header-spacer {
    height: 0;
  }

  body:not(.swimdays-template-index) .swimdays-home-header-spacer {
    height: calc(2rem + clamp(60px, 7vw, 72px));
  }

  body:not(.swimdays-template-index).announcement-hidden .swimdays-home-header-spacer {
    height: clamp(60px, 7vw, 72px);
  }

  @media screen and (max-width: 749px) {
    .swimdays-home-header-spacer {
      height: 0;
    }

    body:not(.swimdays-template-index) .swimdays-home-header-spacer {
      height: calc(2rem + clamp(60px, 7vw, 72px));
    }

    body:not(.swimdays-template-index).announcement-hidden .swimdays-home-header-spacer {
      height: clamp(60px, 7vw, 72px);
    }
  }
{% endstylesheet %}

{% schema %}
{
  "name": "Swimdays home header",
  "tag": "section",
  "class": "shopify-section--swimdays-home-header",
  "limit": 1,
  "settings": [
    {
      "type": "link_list",
      "id": "primary_menu",
      "label": "Primary menu"
    },
    {
      "type": "header",
      "content": "Shipping announcement"
    },
    {
      "type": "checkbox",
      "id": "announcement_enabled",
      "label": "Show shipping announcement slide",
      "default": true
    },
    {
      "type": "text",
      "id": "announcement_text_primary",
      "label": "Announcement primary text",
      "default": "Free Worldwide Shipping"
    },
    {
      "type": "text",
      "id": "announcement_text_secondary",
      "label": "Announcement secondary text",
      "default": "On Orders Over $150"
    },
    {
      "type": "text",
      "id": "announcement_link_label",
      "label": "Announcement link label",
      "default": "Shop now"
    },
    {
      "type": "url",
      "id": "announcement_link_url",
      "label": "Announcement link URL"
    },
    {
      "type": "header",
      "content": "Launch announcement"
    },
    {
      "type": "checkbox",
      "id": "launch_announcement_enabled",
      "label": "Show launch announcement slide",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "launch_announcement_first",
      "label": "Show launch announcement first",
      "default": true
    },
    {
      "type": "text",
      "id": "launch_announcement_label",
      "label": "Launch label",
      "default": "New Arrival"
    },
    {
      "type": "text",
      "id": "launch_announcement_text",
      "label": "Launch text",
      "default": "Sea Spurrey in Pink"
    },
    {
      "type": "text",
      "id": "launch_announcement_link_label",
      "label": "Launch link label",
      "default": "Shop Now"
    },
    {
      "type": "url",
      "id": "launch_announcement_link_url",
      "label": "Launch link URL"
    },
    {
      "type": "color",
      "id": "launch_announcement_background",
      "label": "Launch background",
      "default": "#CFA7BB"
    }
  ],
  "presets": [
    {
      "name": "Swimdays home header"
    }
  ],
  "enabled_on": {
    "templates": ["index", "page", "collection", "product", "blog", "article", "search", "cart", "customers/login", "customers/register", "customers/account", "list-collections"]
  }
}
{% endschema %}
